# Archived negative control. Rejected by the masked-stream safety contract:
# context-only windows cannot update the model or carry their context forward.
extends = "ruliad-r3.stateful-tbptt-base.training.toml"

[training]
tbptt_chunk_size = 512
tbptt_persist_across_steps = false
